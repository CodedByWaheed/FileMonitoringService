2. Build Instructions

Build in Release

Open the solution in Visual Studio.

Then:

Build → Configuration Manager → Release

Make sure the project is selected for Build.

Then:

Build → Build Solution

Or use:

powershell:
dotnet build -c Release

After successful compilation, you should have something similar to:

bin\Release\File Monitoring Windows Service.exe


3. Deployment / InstallUtil Instructions

Because this is a traditional Windows Service using ProjectInstaller.cs , the expected deployment method is (InstallUtil).

Open Command Prompt as Administrator.

Navigate to the Release directory:

cmd
cd "E:\YourProjectPath\File Monitoring Windows Service\bin\Release"


- Install

For a 64-bit .NET Framework installation:

cmd
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\InstallUtil.exe "File Monitoring Windows Service.exe"

If your service is built for 32-bit:

cmd
C:\Windows\Microsoft.NET\Framework\v4.0.30319\InstallUtil.exe "File Monitoring Windows Service.exe"


You should see something similar to:
.
.
.
The Commit phase completed successfully.
The transacted install has completed.
.
.
.


Start the service

cmd
sc start "File Monitoring Windows Service"


Or:

powershell:
Start-Service -Name "File Monitoring Windows Service"


Stop the service

cmd
sc stop "File Monitoring Windows Service"


Or:

powershell
Stop-Service -Name "File Monitoring Windows Service"


Uninstall

First stop it:

cmd
sc stop "File Monitoring Windows Service"


Then:

cmd
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\InstallUtil.exe /u "File Monitoring Windows Service.exe"


4. Test Log

You need to demonstrate that the service actually works.

For example, your test log can look like:

[1448/03/25 15:20:19] File Monitoring Service Started.
[1448/03/25 15:20:42] Renamed file: mock to ff2f9f57-6eab-4731-8e70-d854f0caa6fe.jpg
[1448/03/25 15:20:42] Moved file: mock.jpg to E:\Satge 2\24 - Windows Services\Project1-File Monitoring Windows Service\DestinationFolder
[1448/03/25 15:20:44] Renamed file: Post to dab36127-1418-422e-a696-bf8174b49947.png
[1448/03/25 15:20:44] Moved file: Post.png to E:\Satge 2\24 - Windows Services\Project1-File Monitoring Windows Service\DestinationFolder.

---

# 5. Video Presentation

Your video can be around **5–10 minutes**.

I recommend this exact structure:

### 0:00 – 0:45 — Introduction

Say:

> Hello, my name is Waheed. In this project, I developed a Windows Service for monitoring files in a specified directory. The service automatically detects file-system changes and processes the detected files without requiring a user interface.

Then explain the problem the service solves.

---

### 0:45 – 2:00 — Project Structure

Show Visual Studio and explain:

```text
FileMonitoringService.cs
Program.cs
ProjectInstaller.cs
App.config
```

Explain the responsibility of each file.

For example:

**Program.cs**

Responsible for starting the Windows Service.

**FileMonitoringService.cs**

Contains the main service logic and file monitoring functionality.

**ProjectInstaller.cs**

Allows Windows to install the application as a Windows Service.

**App.config**

Contains configuration such as monitoring paths and other settings.

---

### 2:00 – 3:30 — Code Walkthrough

Open `FileMonitoringService.cs`.

Explain:

* `OnStart()`
* `OnStop()`
* `FileSystemWatcher`
* file event handlers
* logging
* error handling

For example:

```csharp
protected override void OnStart(string[] args)
{
    // Initialize monitoring
    // Start FileSystemWatcher
    // Write startup log
}
```

Then explain your watcher:

```csharp
FileSystemWatcher watcher;
```

and events such as:

```csharp
watcher.Created += ...
watcher.Changed += ...
watcher.Deleted += ...
watcher.Renamed += ...
```

Only show the events your actual project uses.

---

### 3:30 – 4:30 — Installation

Show an Administrator Command Prompt.

Run:

```cmd
InstallUtil.exe "File Monitoring Windows Service.exe"
```

Then:

```cmd
net start "File Monitoring Windows Service"
```

Open:

```text
services.msc
```

Show your service running.

This is an important part of the presentation because it proves that you created an actual Windows Service rather than just a normal console application.

---

### 4:30 – 6:00 — Testing

Open the directory being monitored.

Create:

```text
test1.txt
test2.txt
test3.txt
```

Then show the service reacting to them.

Afterward open your log file and demonstrate entries such as:

```text
File detected: test1.txt
File processed successfully: test1.txt

File detected: test2.txt
File processed successfully: test2.txt

File detected: test3.txt
File processed successfully: test3.txt
```

---

### 6:00 – 7:00 — Design Decisions

Briefly explain why you chose:

* `FileSystemWatcher`
* Windows Service architecture
* configuration through `App.config`
* logging
* exception handling
* separation between installation and service logic

For example:

> I used FileSystemWatcher because it provides an event-driven way to monitor file-system changes instead of continuously polling the directory. This reduces unnecessary CPU usage and allows the service to react when changes occur.

---

### 7:00 – 7:30 — Conclusion

Finish with:

> The final result is a Windows Service that can be installed, started, stopped, and uninstalled using the standard Windows Service deployment tools. The testing demonstrated that the service successfully detects and processes multiple files.

Then stop the recording.


